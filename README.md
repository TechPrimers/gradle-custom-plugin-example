# Gradle Custom Plugin Example

- Created `YoutubePlugin` with task `play` and `pause`
- Created "features" plugin extension (`YoutubePluginExtension`) as a model to pass values from the project to the plugin
